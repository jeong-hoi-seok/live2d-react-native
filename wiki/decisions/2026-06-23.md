# 2026-06-23

## 결정 | 프리미엄 다크 디자인 시스템 적용
- 변경: 외부 `DESIGN.md`의 다크 모드 우선·coral accent 방향을 앱 전역 UI에 반영.
  - `src/shared/lib/design-system.ts`에 색상·spacing·radius·폰트 토큰 추가.
  - `tailwind.config.js`, `global.css`, `navigation-theme.ts`, `app.json`의 배경·surface·accent 색을 디자인 시스템 기준으로 정렬.
  - 홈/대화/MY/알림/캐릭터룸 화면의 하드코딩 색상과 카드 스타일을 토큰 기반으로 교체.
- 이유: 화면별 임시 다크 색상 대신 하나의 디자인 시스템으로 색·대비·컴포넌트 톤을 맞추기 위함.
- 영향: 이후 UI 변경은 `design-system.ts`와 Tailwind `app.*` 색상 토큰을 우선 사용한다.

## 결정 | 홈 화면 character roster 레이아웃 적용
- 변경: 홈 화면을 상단 헤더, 프로모션 배너, 가로 카테고리 칩, 2열 캐릭터 카드, 트렌딩 카드 구조로 개편.
- 유지: 하단 네비게이션, 기존 캐릭터 이미지·이름·설명은 유지.
- 이유: 참조 시안의 premium roster 흐름을 적용하되, 현재 Live2D 캐릭터 콘텐츠와 라우팅 구조는 유지하기 위함.
- 영향: 캐릭터 카드는 별도 하단 정보 패널 대신 이미지 위 하단 그라데이션과 텍스트 오버레이를 사용한다.

## 결정 | 외부 디자인 원문 폰트 제거 및 Pretendard 유지
- 기존: 디자인 시스템 원문은 별도 라틴 폰트를 지정.
- 변경: 앱 폰트는 Pretendard만 사용하고, 추가했던 외부 폰트 파일과 참조를 제거.
- 이유: 해당 폰트는 한글 glyph 렌더링이 깨져 홈 텍스트가 자모처럼 보였다. 앱 주요 문구가 한국어라 Pretendard가 더 안정적이다.
- 영향: `FONT_ASSETS`, `global.css`, `tailwind.config.js`는 Pretendard만 참조한다.

## 결정 | 위키·문서를 외부 옵시디언 볼트로 단일화
- 기존: 인-레포 `wiki/`(design-tokens·splash-screen·index), `docs/`(ai-wiki-guide·commit-convention·naming-convention)에 컨벤션·문서 분산.
- 변경: 컨벤션·개발원칙·디자인시스템·기술개념은 외부 옵시디언 볼트 `/Users/hoi-seok/project/llm-wiki`를 단일 진실 원천(SSOT)으로. 인-레포 `wiki/`·`docs/` 삭제.
- 이유: 같은 프론트엔드 지식이 프로젝트마다 중복·표류. 볼트 1곳에서 관리하면 여러 프로젝트가 공유·재사용.
- 영향:
  - `agent.md`가 볼트를 SSOT로 참조하도록 갱신 (작업 전 볼트 먼저 읽기).
  - `README.md` 문서 안내 정리.
  - **프로젝트 고유 맥락(decisions·worklog)은 볼트가 아니라 이 레포 root `wiki/`에 남긴다.** 볼트는 범용 지식, 레포 `wiki/`는 이 프로젝트만의 결정 이력. 이 파일이 그 첫 항목.

## 결정 | 다크 테마 기본값 적용
- 변경: 앱 전역 다크 테마를 기본값으로 고정.
  - `app.json`: `userInterfaceStyle` `automatic` → `dark`, `backgroundColor: "#1a1a1a"` 추가.
  - `src/shared/lib/navigation-theme.ts` 신설 — `@react-navigation/native` `DarkTheme` 확장 (`APP_BACKGROUND #1a1a1a`, `APP_SURFACE #1f1f1f`, text/primary `#dadada`, border `#2a2a2a`, notification `#ef4444`).
  - `src/app/_layout.tsx`: `ThemeProvider`로 `APP_DARK_THEME` 주입, `StatusBar style="light"`, Stack `contentStyle` 배경 통일.
- 이유: 시스템 따라가는 `automatic` 대신 다크 1개 기준으로 색·대비를 한 번에 통제. 네비게이션 색을 `navigation-theme` 한 곳에 모아 화면별 하드코딩 제거.
- 영향: 탭 레이아웃·profile·character-list 색이 공통 테마 토큰으로 정렬. 추후 라이트 테마 필요 시 같은 모듈에 테마 추가로 확장.

## 결정 | 알림 설정 feature 신설
- 추가: `src/features/notification-settings/` (FSD feature) — `model/use-notification-settings-store.ts`(상태), `ui/notification-settings-screen.tsx`(UI), `index.ts`(public API).
- 라우트: `src/app/settings/notifications.tsx` 신설.
- 맥락: settings 영역 시작점. 설정류 화면은 `src/app/settings/` 하위 라우트 + `features/*-settings` feature로 분리하는 패턴 채택.
